#!/usr/bin/make -f

# Экспорт переменных из CMake
export PROJECT_NAME = SQLite3Server

%:
	dh	$@

override_dh_auto_configure:
	dh_auto_configure -- -DCMAKE_BUILD_TYPE=Release

override_dh_auto_install:
	dh_auto_install	
